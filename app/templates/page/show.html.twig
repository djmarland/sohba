{% extends "base.html.twig" %}

{% block title %}{{ page.title }} - {% endblock %}

{% block body %}
    <div class="text--prose">
        <h1>{{ page.title }}</h1>
        {{ page.htmlContent|raw }}
    </div>
{% endblock %}